(1) movb $0xF, (%ebx)  - 명령어 접미사와 목적지 레지스터 크기가 다름. movl을 사용해야함.
(2) movl %rax, (%rsp) - 명령어 접미사가 잘못됨. movq를 사용해야 함.
(3) movw (%rax),4(%rsp) - 명령어 접미사가 소스 레지스터, 목적어 레지스터의 크기와 다름. 메모리에서 메모리로 바로 이동시키는 명령어는 없음.
(4) movb %al,%sl - 목적지 레지스터의 sl은 없는 레지스터. %sil를 잘못 표기한 것으로 보임.
(5) movq %rax,$0x123 - mov 클래스의 명령어는 immediate value를 목적지로 설정할 수 없음.
(6) movl %eax,%rdx - 목적지 레지스터의 크기가 잘못되었음. %edx가 되어야 함.
(7) movb %si, 8(%rbp) - 명령어 접미사와 소스 레지스터의 크기가 맞지 않음. movw로 고치거나 %sil로 고쳐야 됨.